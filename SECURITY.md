# 安全与隐私

## 公开仓库不包含的内容

- Qwen3-VL或其他模型权重；
- 企业产品手册、内部销售资料和项目文件；
- 客户上传文件与聊天记录；
- API Key、访问令牌和本地环境文件；
- 私有RAG索引、视觉向量、运行日志和评测数据。

## 本地运行原则

- 企业RAG和生成模型默认在本地设备运行；
- 客户附件仅进入进程内存会话，默认一小时过期；
- 临时视觉文件在一次推理完成后删除；
- 客户附件不自动写入公司知识库；
- 联网检索只发送当前公开问题，不发送私有Evidence内容。

## 部署建议

- 生产环境不要使用 `FACADE_PUBLIC_FRONTEND=*`；
- API应部署在身份认证或VPN之后；
- 不要把 `.env`、模型目录和真实 `data/sales/` 内容提交到Git；
- 对公开图片接口增加业务身份认证、访问日志和速率限制；
- 使用前确认企业资料、标准文件和第三方数据的授权范围。

如发现安全问题，请不要公开提交包含密钥或客户数据的Issue。
