# 内部资料区

当前内部知识库为空，现有建材产品、施工、节点、检测和案例资料均按业务负责人确认标记为 `public`。

后续内部资料入库时必须显式设置：

```json
{
  "access_scope": "internal"
}
```

`document_category` 只描述资料的业务用途，不承担访问控制。匿名用户只能检索 `public`；管理员授权的内部成员才能同时检索 `public` 与 `internal`。

不要把客户临时上传附件放入此目录。客户附件仍绑定临时会话，不进入企业知识库。
